## Executive assessment

The production release gate is green for installation, unit tests, strict TypeScript, production build, dependency audit, and peer dependencies. The remaining ESLint backlog is substantial but is dominated by formatting drift rather than runtime correctness. A repository-wide ESLint run over `src`, `server`, `vite.config.ts`, and `eslint.config.js` reported **722 problems: 709 errors and 13 warnings**. Of those, **664 errors and 0 warnings are automatically fixable** with ESLint’s `--fix` option, and the largest class is `prettier/prettier`.

The backlog should not be treated as one undifferentiated cleanup. Formatting can be automated in a controlled branch, while type-safety, authentication, payment, webhook, and route-safety findings require focused code review and tests.

## Priority register

| Priority                 | Item                                                                                                 | Evidence                                                                                                                                                                                                                                 | Why it matters                                                                                                                                            | Recommended action                                                                                                                                                                                                                              |
| ------------------------ | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0 developer launch      | Ship the self-serve developer portal, API key lifecycle, credit wallet and verified-model playground | `src/routes/app.developers.tsx`, `src/lib/api/api-keys.ts`, `src/routes/app.models.tsx`, `server/api/v1/generate.ts`                                                                                                                     | Developers are now the primary economic actor; admin-only access, hidden categories and hardcoded recharge caps would block the intended product          | Deploy the tranche, run authenticated browser smoke, create/revoke a key, verify a 1 USD recharge path and test one API call without exposing the raw key                                                                                       |
| P0 developer security    | Enforce API key scopes and verified models on every public generation path                           | `server/api/v1/generate.ts`, `server/api/v1/models/index.ts`, `server/api/v1/workflows/run.ts`, `src/lib/api-key-policy.ts`                                                                                                              | A key must access all verified models by default while restricted keys must not bypass category permissions or model verification                         | Keep server-side checks, add negative tests for inactive keys, wrong scopes, unverified slugs and workflow bypasses                                                                                                                             |
| P1 developer integration | Add formal application binding, key rotation, usage limits and a first-party SDK                     | `api_keys` currently stores name, hash, prefix, permissions and status but no application/environment record                                                                                                                             | Naming a key is a useful first link, but production teams need rotation, environment separation and operational ownership                                 | Add an additive schema and API contract only after the core developer flow is observed with real users                                                                                                                                          |
| P0 before live cutover   | Remove or protect `/run-migration`                                                                   | `src/routes/run-migration.tsx` calls a GET server function that mutates the database; `src/lib/api/run-migration.ts` has no visible authentication or admin gate                                                                         | A public GET can execute schema-changing SQL and the helper is not equivalent to the checked-in migration chain                                           | Remove the route from production, or require a server-side admin gate and convert migration execution to an explicit operator-only command. Do not use it for `0020`.                                                                           |
| P0 before live cutover   | Correct the production environment documentation                                                     | `PRODUCTION-RUNBOOK.md` names `R2_ACCOUNT_ID` and `R2_BUCKET_NAME`, while `src/lib/storage/r2.ts` uses `R2_ENDPOINT`, `R2_BUCKET`, and `R2_REGION`                                                                                       | A wrong secret name can silently disable durable media storage or cause failed generations                                                                | Update the runbook and deployment configuration using the source-of-truth names listed in `LIVE-CUTOVER-CHECKLIST.md`.                                                                                                                          |
| P1 after staging gate    | Make the bounded canvas agentic flow the next product branch                                         | `src/components/canvas/agent-panel.tsx`, `src/lib/agent.ts`, and `src/lib/api/agent-apply.ts` already cover proposal, verified-model filtering, cost confirmation, server-side application, conversation persistence, and canvas refresh | It is the clearest next step toward a unified creative workspace, but it must not outrun payment/generation proof or become an unbounded autonomous agent | Prove one end-to-end staging workflow, then observe 3–5 workflows with cost, permission, partial-failure, retry, and ownership evidence. Keep `ADD_NODE`, `CONNECT_NODES`, `UPDATE_NODE`, and `REMOVE_NODE` bounded.                            |
| P1                       | Reduce authentication type assertions                                                                | `src/routes/auth.$pathname.tsx` contains several `any` casts and an empty fallback branch; the current ESLint run reports explicit-any and `no-empty` findings                                                                           | Auth code is security-sensitive and difficult to review when response shapes are weakened                                                                 | Introduce typed Neon Auth response adapters, remove `any`, and make the no-password fallback explicit. Add sign-in, verification, and session-cookie tests.                                                                                     |
| P1                       | Remove explicit `any` from canvas agent and API helpers                                              | 21 explicit-any matches across `agent-panel.tsx`, `param-editor.tsx`, `agent-apply.ts`, `agent-conversations.ts`, `workflows.ts`, `db.ts`, `app.models.$slug.tsx`, and auth                                                              | Dynamic AI payloads are a common source of malformed requests and silent provider failures                                                                | Define discriminated unions for model parameters, provider responses, and agent plans. Use `unknown` at boundaries and validate with Zod v4.                                                                                                    |
| P1                       | Fix React hook dependency warnings                                                                   | Warnings include missing `loadedRef`, `t`, and `model.category` dependencies                                                                                                                                                             | Stale closures can create incorrect generation parameters, stale translations, or inconsistent canvas initialization                                      | Resolve each warning individually; do not blanket-disable `react-hooks/exhaustive-deps`. Add focused regression tests for canvas initialization and model sorting.                                                                              |
| P1                       | Replace client-side API example interpolation                                                        | `app.developers.tsx` displays `process.env.CORTEXIA_KEY` inside sample JavaScript strings                                                                                                                                                | This is documentation text, not a valid browser runtime secret, and can confuse developers about where keys belong                                        | Replace with `process.env.CORTEXIA_KEY` only inside displayed code examples and document that real API keys are user-created developer keys; do not add `CORTEXIA_KEY` as a deployment secret unless a separate server integration requires it. |
| P1                       | Add structured logging and correlation IDs                                                           | Source contains 58 `console.error`, 17 `console.log`, and 1 `console.warn` calls                                                                                                                                                         | Payment, webhook, and generation incidents need searchable event IDs without leaking payloads or secrets                                                  | Add a small server logger with request ID, provider event ID, task ID, user ID, and severity fields. Redact authorization headers, signatures, prompts containing sensitive data, and payment secrets.                                          |
| P1                       | Validate provider callback response-time behavior                                                    | KIE, FedaPay, and Stripe callbacks currently perform database work inline                                                                                                                                                                | Provider documentation expects fast 2xx acknowledgement and retries non-2xx responses                                                                     | Keep handlers bounded and idempotent; move heavy media processing to durable jobs if callback volume increases. Instrument latency and retry counts before scaling.                                                                             |
| P2                       | Automate formatting cleanup                                                                          | 664 ESLint errors are auto-fixable and most are `prettier/prettier`                                                                                                                                                                      | Formatting noise obscures functional review and blocks a clean lint gate                                                                                  | Run Prettier/ESLint autofix in a dedicated mechanical commit, then review the diff and add it as a CI gate. Do not mix the formatting-only commit with billing or auth logic.                                                                   |
| P2                       | Refactor Fast Refresh exports                                                                        | 10 warnings are `react-refresh/only-export-components`                                                                                                                                                                                   | Mixed component/constant exports can weaken hot-reload behavior and create development-only surprises                                                     | Move constants and helper functions into adjacent modules; keep route/component modules component-focused.                                                                                                                                      |
| P2                       | Replace route-level API calls with typed shared clients                                              | Several routes directly call server functions with repeated session-token payload shapes                                                                                                                                                 | Repeated contracts increase schema drift and make auth hardening inconsistent                                                                             | Centralize typed server functions and remove obsolete `sessionToken` fields now that the HttpOnly session cookie is authoritative.                                                                                                              |
| P2                       | Add integration coverage for payments and callbacks                                                  | Current deterministic suite covers payment references, FedaPay signatures, and KIE task-ID extraction; it does not exercise a real database transaction or provider replay                                                               | Unit tests cannot prove unique-index behavior, atomic crediting, or full reconciliation                                                                   | Add staging-backed tests for approved, declined, duplicate, mismatched-amount, and replayed callbacks.                                                                                                                                          |
| P2                       | Improve repository release scripts                                                                   | `package.json` has no migration or CI script; `lint` is repository-wide and currently noisy                                                                                                                                              | Operators need one reproducible release command and a clear migration path                                                                                | Add `check:release`, `db:migrate:0020`, and CI jobs for frozen install, tests, TypeScript, build, audit, and changed-file lint.                                                                                                                 |
| P3                       | Remove deprecated and inactive package debt                                                          | Install reports 21 deprecated React Email subdependencies and Recharts 2.x is inactive                                                                                                                                                   | It increases upgrade friction and future security-review noise                                                                                            | Upgrade Recharts and the email stack in separate compatibility projects after launch metrics are stable.                                                                                                                                        |
| P3                       | Improve i18n completeness and product copy                                                           | The application maintains large multilingual dictionaries and contains “Coming soon” copy                                                                                                                                                | Missing or stale translations reduce launch quality but do not block the core payment/generation path                                                     | Add translation-key parity checks and replace placeholder copy for post-launch product surfaces.                                                                                                                                                |

## Recommended execution sequence

The first product window should ship the self-serve Developer Platform: remove the admin-only developer gate, allow all verified model categories in the authenticated catalogue/playground, enforce scopes server-side, and keep the credit minimum at 1 USD without a Cortexia hard maximum. In parallel, retain the migration and payment safety work. The next window should run authenticated production smoke checks and payment/API reconciliation. Only after the developer flow is observed and stable should the team expand into formal application binding, SDKs, Canva-like orchestration or autonomous agents.

## Release-gate policy after launch

A pull request that touches authentication, credits, payment orders, provider webhooks, storage, or model pricing should pass frozen installation, unit tests, strict TypeScript, production build, dependency audit, changed-file ESLint, and a staging smoke test. Formatting-only changes may be merged separately, but they must not be used to conceal functional changes.
